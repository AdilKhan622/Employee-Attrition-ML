# Employee Attrition Prediction

This project analyzes HR data to predict employee attrition using various Machine Learning algorithms (Logistic Regression, Decision Tree, SVM, Random Forest, etc.).

## 📂 Project Structure

- `codes/` – Python scripts for model training and evaluation
- `data/` – Dataset used
- `report/` – Project report (PDF, DOCX)
- `README.md` – Overview of the project

## ⚙️ Requirements

```bash
pip install -r requirements.txt
```
